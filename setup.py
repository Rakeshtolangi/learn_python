from setuptools import setup
setup(
    name="rakesh_software",
    version=1.0,
    packages =['rakesh']
    )

    # pip install . 
    # . resembles setup
    # if you use random name than setup, you can use name rather than . sign